var app = require("./app");

window.AboutController = {
    init: function () {
        
    }
};

window.addEventListener('load', function () {
    window.App.start();
    window.AboutController.init();
});