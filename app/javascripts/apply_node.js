var app = require("./app");

window.ApplyNodeController = {
    init: function () {
        
    }
};

window.addEventListener('load', function () {
    window.App.start();
    window.ApplyNodeController.init();
});