var app = require("./app");

window.ProjectDetailsController = {
    init: function () {
        
    }
};

window.addEventListener('load', function () {
    window.App.start();
    window.ProjectDetailsController.init();
});