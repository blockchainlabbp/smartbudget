var app = require("./app");

window.CreateNodeController = {
    init: function () {
        
    }
};

window.addEventListener('load', function () {
    window.App.start();
    window.CreateNodeController.init();
});